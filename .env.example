# ─── Supabase (use pact_app role — not postgres superuser) ───
# Vercel/serverless: use pooled URL (port 6543) for DATABASE_URL
# Migrations: use direct URL (port 5432) for DIRECT_URL
DATABASE_URL="postgresql://pact_app.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://pact_app.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# Not used by the app (Prisma only) — safe to omit in production
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# ─── Auth ───────────────────────────────────────────────────
# Set both to the same value (generate with: openssl rand -base64 32)
AUTH_SECRET=""
NEXTAUTH_SECRET="generate-with: openssl rand -base64 32"
NEXTAUTH_URL="https://your-app.vercel.app"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ─── Stripe ─────────────────────────────────────────────────
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
# Optional — not used by current Checkout flow
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# ─── Email (Resend) ─────────────────────────────────────────
# https://resend.com/api-keys
RESEND_API_KEY=""
EMAIL_FROM="Pact <onboarding@resend.dev>"
EMAIL_REPLY_TO="use.pact.features@gmail.com"

# ─── Vercel Blob (lease PDF uploads) ────────────────────────
# Create in Vercel → Storage → Blob
BLOB_READ_WRITE_TOKEN=""

# ─── App ─────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
